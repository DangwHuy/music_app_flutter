name: lan2tesst
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  # ADDED: Google AI package for Gemini
  google_generative_ai: ^0.4.0
  timeago: ^3.6.1
  share_plus: ^7.2.1 #chia se profile
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.4
  firebase_storage: ^11.7.7
  image_picker: ^1.1.2
  firebase_messaging: ^14.9.3
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  story_view: ^0.16.0 # (hoặc phiên bản mới nhất)
  cached_network_image: ^3.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/songs.json
  fonts:
    - family: Billabong
      fonts:
        - asset: assets/fonts/Billabong.otf
